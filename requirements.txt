# Core libraries
pandas>=1.5,<3.0
requests>=2.31
pyarrow>=12.0
numpy>=1.21,<2.0
scipy>=1.9.0

# Backtesting
vectorbt>=0.25.0

# Exchange clients
python-okx>=0.2.1
python-binance>=1.0.32
ccxt>=4.5.0

# Utilities
tabulate>=0.9.0
mplfinance>=0.12.10b0
plotly>=5.0
python-levenshtein>=0.21
seaborn>=0.12
notebook>=7.0
ipykernel>=6.0

# Monitoring / Dashboard (optional, used on cloud server)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Persistence (PostgreSQL)
psycopg[binary,pool]>=3.2.0

# Optional technical indicators (install if available on platform)
TA-Lib>=0.4.26; platform_system != "Windows" or platform_machine != "ARM64"

